class Solution:
    def mySqrt(self, x: int) -> int:
        mid=sqrt(x)
        midd= math.floor(mid)
        return midd

        # if x < 2:
        #     return x
        # left = 1
        # right = x
        # answer = 0
        # while left <= right:
        #     mid = (left + right) // 2
        #     square = mid * mid
        #     if square == x:
        #         return mid
        #     elif square < x:
        #         answer = mid
        #         left = mid + 1
        #     else:
        #         right = mid - 1

        # return answer