class Solution:
    def maxCoins(self, piles: List[int]) -> int:
        piles.sort()
        n = len(piles) // 3
        total = 0
        index = len(piles) - 2
        for i in range(n):
            total += piles[index]
            index -= 2
        
        return total